# Label Studio Configuration
# Copy this file to .env and modify as needed

# PostgreSQL connection (requires postgres-container running)
# First create the database:
# docker exec -it postgres-container psql -U postgres -c "CREATE DATABASE labelstudio;"

POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
LABELSTUDIO_DB=labelstudio

# Label Studio host (for proper URL generation)
LABEL_STUDIO_HOST=http://localhost:8082

# Initial admin credentials (used on first startup)
LABEL_STUDIO_USERNAME=admin@example.com
LABEL_STUDIO_PASSWORD=admin

# Access Label Studio at: http://localhost:8082

# Features:
# - Data annotation and labeling
# - Image, text, audio, video, time-series support
# - Machine learning model integration
# - Export in various formats (JSON, CSV, COCO, YOLO, etc.)
# - Multi-user collaboration
# - API for automation

# Python SDK:
# pip install label-studio-sdk
# from label_studio_sdk import Client
# ls = Client(url='http://localhost:8082', api_key='YOUR_API_KEY')
