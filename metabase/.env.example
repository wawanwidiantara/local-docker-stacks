# Metabase Business Intelligence Configuration
# Copy this file to .env and modify as needed

# PostgreSQL connection (requires postgres-container running)
# First create the database:
# docker exec -it postgres-container psql -U postgres -c "CREATE DATABASE metabase;"

POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
METABASE_DB=metabase

# Encryption secret key (generate with: openssl rand -base64 32)
MB_ENCRYPTION_SECRET_KEY=your-secret-key-here-change-this-in-production

# Access Metabase at: http://localhost:3001
# First time setup will prompt you to create an admin account

# Connect to your databases:
# - PostgreSQL: postgres-container:5432
# - MySQL: mysql-container:3306
# - MongoDB: mongo-container:27017
